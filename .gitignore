# Personal save data — never tracked, never pushed, never overwritten by pulls
data/

# Python cache
__pycache__/
*.pyc

# PyInstaller build output
build/
dist/
*.spec

# In case the compiled updater exe is ever run from inside the repo folder,
# it creates app\ (a copy of the source) next to itself — never track that
app/